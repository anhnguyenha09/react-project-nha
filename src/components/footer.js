import './Footer.css';

function Footer() {
    return (
        <footer className="footer">
            <p>© {new Date().getFullYear()} -- Quan ly sinh vien --</p>
        </footer>
    );
}

export default Footer;
